/* low battery charge - notify we're low. */
#define BATLOW 20
#define LOWMSG "Battery is low."

/* the interval (in seconds) the daemon checks the battery. */
#define INTERVAL 45

/* Some visual things for the window. */
/* x-padding for window. */
#define WPADX 12
/* y-padding for window. */
#define WPADY 10
/* vertical gap between text and button. */
#define WGAP 16

/* x-padding for button. */
#define BPADX 25
/* y-padding for button. */
#define BPADY 12

static char* fontname = "-*-terminus-medium-r-normal--28-*-*-*-c-*-iso10646-1";
